# Linting is now handled by ci.yml — this file is kept empty to avoid confusion.
# It will be removed in a future cleanup.
name: Lint (deprecated)

on:
  workflow_dispatch:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Linting is now part of the CI workflow. See ci.yml."
